import * as codeOSSBuffer from '@pkerschbaum/code-oss-file-service/out/vs/base/common/buffer';

export const VSBuffer = codeOSSBuffer.VSBuffer;
export type VSBuffer = codeOSSBuffer.VSBuffer;
export type VSBufferReadable = codeOSSBuffer.VSBufferReadable;
export type VSBufferReadableStream = codeOSSBuffer.VSBufferReadableStream;
