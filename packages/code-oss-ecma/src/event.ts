import * as codeOSSEvent from '@pkerschbaum/code-oss-file-service/out/vs/base/common/event';

export const Emitter = codeOSSEvent.Emitter;
export type Emitter<T> = codeOSSEvent.Emitter<T>;
export type Event<T> = codeOSSEvent.Event<T>;
