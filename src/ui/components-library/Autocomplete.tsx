import {
  Autocomplete as MuiAutocomplete,
  createFilterOptions as createMuiFilterOptions,
} from '@mui/material';

export const Autocomplete = MuiAutocomplete;
export const createFilterOptions = createMuiFilterOptions;
